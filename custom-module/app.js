const multiply = (a ,b) => a * b

const divide = (a ,b) => a / b

// export const mathOperation = {multiply , divide} // this is for naimly export 

const mathOperation = {multiply , divide}

export default mathOperation